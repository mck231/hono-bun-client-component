{
  "name": "my-app",
  "scripts": {
    "dev": "bun run --hot src/index.tsx",
    "bundle": "bun build src/client.tsx --outdir ./static --outfile client.js"

  },
  "dependencies": {
    "hono": "^4.5.11"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}